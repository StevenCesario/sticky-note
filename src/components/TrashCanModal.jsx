import React from 'react'

const TrashCanModal = ({ onClose }) => {
  return (
    <div>
      TrashCanModal
      <button type='button' onClick={onClose}>Close</button>
    </div>
  )
}

export default TrashCanModal